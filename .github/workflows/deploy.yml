name: testing
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  label:
    types:
      - created
jobs:
  calculation:
    runs-on: ubuntu-latest
    steps:
      -  run: echo ${{ toJSON(github.event) }}