name: Testing Composite Actions
on:
  push:
    branches:
      - main
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/cache-dpe
        id: composite-action
        with:
          name: 'Devansh Popli'
          age: 24
      - run: echo 'output is ${{ steps.composite-action.outputs.result }}'