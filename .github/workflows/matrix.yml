name: Matrix workflow
on: push
env:
  MY_ENV: ${{ contains('Hello World','lloe') }}
  NEW_ENV: ${{ toJSON(github)}}
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo ${{ env.MY_ENV }} and ${{ env.NEW_ENV }}