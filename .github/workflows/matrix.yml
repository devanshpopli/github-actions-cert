name: Matrix workflow
on: push
env:
  MY_ENV: ${{ contains('Hello World','llo') }}
  NEW_ENV: ${{ joins('Hello World',',') }}
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo ${{ env.MY_ENV }} and ${{ env.NEW_ENV }}