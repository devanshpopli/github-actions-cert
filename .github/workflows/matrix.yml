name: Matrix workflow
on: push
env:
  MY_ENV: null
  MY_ENV2: ${{ null }}
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo ${{ env.MY_ENV }} and ${{ env.MY_ENV2 }}