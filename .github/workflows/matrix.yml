name: Permissions Workflow
on: 
  push:
    branches:
      - main
jobs:
  Build:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3      
      