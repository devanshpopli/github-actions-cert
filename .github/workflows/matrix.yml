name: Permissions Workflow
on: 
  push:
    branches:
      - main
jobs:
  Build:
    permissions:
      issues: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3      
      