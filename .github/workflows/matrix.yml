name: Matrix workflow
on: push
env: 
  NAME: Devansh Popli
jobs:
  Build:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'push' }}
    steps:
      - run: echo ${{ github.actor }} ${{ github.job }} ${{ github.action }} ${{ github.toJSON(event) }} ${{ github.sha }} ${{ github.repository }} ${{ github.ref }}