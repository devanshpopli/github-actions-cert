name: Workflow Example
run-name: ${{ github.actor }} has created this workflow!
on:
  workflow_run:
    workflows: ['Website Deployment']
    branches: ['main']
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.3.0
      - uses: actions/setup-node@v3.6.0
